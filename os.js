import os from 'os';


//userInfo()

console.log(os.userInfo());


//totalmem() - total memory in bytes

console.log(os.totalmem());

//freemem() - free memory in bytes

console.log(os.freemem());

//cpus() - array of objects for every cpu core

console.log(os.cpus());
